"""
Arquivo de testes legado (Flask) movido para `legacy/test_api_flask.py`.
Crie novos testes usando `fastapi.testclient` e `pytest`.
"""
import sys

print("Legacy Flask tests moved to legacy/test_api_flask.py. Use FastAPI tests instead.")
sys.exit(0)
